{
  "name": "endless-runner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate:assets": "node scripts/validate-assets.mjs",
    "fix:glb": "node scripts/fix-glb-chunk.mjs",
    "build": "npm run validate:assets && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "postprocessing": "^6.39.4",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
